noArgs: doSomething()

withArgs: calculate(1, 2, 3)

namedArgs: create(x: 10, y: 20, label: "point")

nestedCall: outer(inner(value))


x: fn(...args) -> {
    
}

y: struct(Element, SomeOther)

d: choice(red:, green:, blue:)

x: fn(things) -> Int {}
